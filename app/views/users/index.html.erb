<div class='center'>
    <div>
        <%= "Total number of users: #{@users.length}" %>
    </div>
    <% @users.each do |user| %>
        <div class='userDiv'>
            <div>
                <%= image_tag user.photo, width:40*40 %>
            </div>
            <div class='border flex'>
                <%= link_to user.name, user %>
                <div class='bottom_flex'>
                    <%= "Number of posts: #{user.post_counter}" %>
                </div>
            </div>
        </div>
    <% end %>
</div>